import { RestrictDecimalDirective } from './restrict-decimal.directive';

describe('RestrictDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
