export enum DateFilter {
  EARLIEST = 'date_earliest',
  LATEST = 'date_latest',
}

export enum AmountFilter{
  HIGHEST="amount_highest",
  LOWEST="amount_lowest"
}

export enum CrifStages {
  CRIF = 'crif',
  RECALL = 'recall',
  UPDATE_CRIF = 'update-crif',
  ELIGIBILITY = 'eligibility',
  SUCCESS = 'success',
  FAILURE = 'failure',
}
